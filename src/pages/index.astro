---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";
import createSlug from "../lib/createSlug"

const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-12 mt-5">
    <div class="text-xl py-1">Hey there 👋</div>
    <div class="text-5xl font-bold">I'm Jon Kaplan</div>
    <div class="text-3xl py-3 font-bold">Product Manager and Engineer</div>
    <div class="py-2">
      <text class="text-lg">
        <!-- long text here test--> 
      </text>
    </div>
    <div class="mt-8">
      <a class="btn" href="https://www.linkedin.com/in/jonathankaplan/" target="_blank"> Let's connect!</a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">My last projects {"</>"}</div>
  </div>

  <HorizontalCard
    title="Wing Uncrewed Traffic Management"
    img="/Wing Delivery Drone.webp"
    desc="Wing is a graduate of Google X and a current Alphabet Bet."
    url="https://wing.com/unmanned-traffic-management/"
    badge=""
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="FASTER Wearable Robotics Product Line"
    img="/FASTER.jpg"
    desc="The FASTER Product line was a novel plug and play set up to rapdily enable military and academic researchers to repeatably conduct research on wearable robotics systems. Powered the Open Source Leg project: https://opensourceleg.com/"
    url="https://exoskeletonreport.com/product/exoboot/"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Demo Project 3"
    img="/post_img.webp"
    desc="Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua."
    url="#"
    badge="FOSS"
  />

  <!-- 
  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-10">Latest from blog</div>
  </div>


  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + createSlug(post.data.title, post.slug)}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  }

  -->

</BaseLayout>
